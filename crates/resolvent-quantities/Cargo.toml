[package]
name = "resolvent-quantities"
version = "0.1.0"
edition = "2024"
rust-version = "1.93"
license = "MIT OR Apache-2.0"
description = "Physics-neutral quantity, unit, and bound semantics for Resolvent"

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"
